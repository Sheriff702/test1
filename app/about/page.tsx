import { AboutContent } from "@/components/about/AboutContent";

export const metadata = {
  title: "About — MARES",
  description: "The studio, the process, the uniform.",
};

export default function AboutPage() {
  return <AboutContent />;
}
